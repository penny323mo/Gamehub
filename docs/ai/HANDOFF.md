# Current cross-agent handoff

Updated: 2026-08-05 (Asia/Macau)
Prepared by: Claude Code (cloud)
Integration branch: `main`
Work branch: `main`
Status: Elden Ring II 個場搬咗出 `map.ts`，喺 Node 度 270ms 量到 (ADR-167)；**MOBA balance 仍然紅**

## Current objective

Make the MOBA hold up on Penny's phone. Not finished; this is a tested checkpoint.

## Completed

### Elden Ring II：個場搬出 React effect，喺 Node 度 270 毫秒量得晒 (ADR-167)

- 之前每一條關於地圖嘅問題都要開真瀏覽器答，因為個場住喺 `useEffect` 入面。軟件光柵化一秒三幀、角色
  一秒行半米，所以「敵人追唔到我」同「地圖爛咗」係同一個量度（ADR-157 就係為咗呢個放棄咗會玩嘅 bot），
  而 ADR-165 嗰個鏡像約定捱咗咁多輪都係因為**得一把尺，而嗰把尺貴到冇人肯行第二次**。
- `src/map.ts` 而家淨係講個場係咩形狀：常數 + `buildMap(add)`。**唔 import three 亦唔 import
  cannon-es**，擺盒嗰個動作由外面傳——遊戲傳「開一個 CANNON body」，測試傳「記低佢」，即係喺 Node 度
  砌得返成個碰撞世界。搬完之後由行緊嘅遊戲 dump 出嚟嘅 111 個盒**逐字一樣**。
- `tests/map.test.mjs`：**270 毫秒**（瀏覽器套件六分半），問三樣之前貴到唔問得嘅嘢——三個場到唔到、
  可行區域**封唔封得住**（地板係無限平面，「行唔行得出去」係真問題而從來冇問過）、每道環牆嘅開口數目
  同角度。
- **三條入面兩條第一次跑就紅，兩條都係支尺**：探點跌咗喺格網外面，索引滑咗去隔籬行讀到隔籬格（假嘅
  「行得出場外」）——而家探點出界就直接掟錯；掃描由角度 0 開始，跨過 0 嗰個東門被斬成**兩個**開口
  （0.05 同 6.23）——而家接返埋先數。
- 突變全部秒級：封死圓場北門 → `1 個開口，應該 2`；橋欄杆短三米 → `漫咗出場外 (0, 45)`；轉返鏡像
  約定 → 三條一齊紅。封北門**唔會**令聖所去唔到（L 形捷徑照到），所以兩條 gate 唔重複。

### 上兩輪：撞到嘅牆望唔見、望到嘅牆停唔到 (ADR-165/166)

- **量「撞得到嘅嘢望唔望得見」**：93 個 collider、612.2 米，**187.6 米（30.6%）一米半內冇任何睇得見嘅
  嘢**，20 個成條長度隱形。地圖輪廓寫咗兩次——collider 由 `BRIDGE`／`HALL`／`LINK` 生出，畫面嗰啲
  `wall.glb` 係另一張手寫表（四個位隔 7 米而模型闊 3.97 米，**每兩幅之間三米望落空但照撞**），三個圓
  場嘅 85 個環牆更加冇網格。而家 **collider 表本身就係網格表**（一個 InstancedMesh 同一個 loop 出）。
- **反方向**：(9, 15) 有盒冇模型（出生點行兩步撞到隱形嘢），而**同一個模型一個場實心一個場穿得過**。
  collider 而家由模型自己 **2 米以下嗰截**度——用成個 AABB 唔得，樹嘅 AABB 係樹冠。
- **霧門一直攔緊空氣**：地圖砌成環之後多咗第二條路入 boss 場，霧門淨係企喺北面通道，**一關都唔使打就
  入到 boss 場**。舊 gate 淨係沿 x=0 行，即係淨係行霧門嗰條路。而家兩道門、一份尺寸、一齊升降。
- **走廊裝飾牆內面喺 collider 面入面 1.36 米**（ADR-166）：望住幅牆行埋去會喺牆入面成米幾先停。而家
  `鋪一排()` 由走廊自己嗰組數擺位、量返模型自己幾深、鋪 `round(長/闊)` 塊。28 → 60 塊，差 → 0.00。
  代價量咗：同一個 32 秒窗口郁動秒 6.0 → 5.9，**低過 2%**。
- **支尺自己錯咗三次，全部靠突變捉到**：轉角約定鏡像咗（五份抄本一樣錯；掃環牆啱嘅兩個空隙、錯嘅報
  二十個——**鏡像返轉頭嘅環仲係一個環**）；分界線判斷冇限 x；出手節奏 gate 一下出手值 0.26/秒而門檻
  0.9（3 下綠 4 下紅），改咗量兩下出手之間嘅郁動秒。ADR-166 條 gate 第一版符號寫反，突變之下照綠。

### 之前幾輪：版位、像素、地圖成環、時鐘 (ADR-150–164)

- **右上角嗰疊 (ADR-163/164)**：`ENTER THE VEIL` 兩個手機打橫得 **30px**、♪ 同 © 五個尺寸全部
  **32×32**，高度全部係副產品。加大之後又整爛咗遊戲入面個 HUD——右邊三個唔相干嘅常數（掣 top 82、
  高 32、面板 top 103），**本來已經疊住**只係啱好未掂到字。面板個 top 而家計出嚟。**呢一輪嘅缺陷係
  呢一輪自己個修正整出嚟。** credits 嗰啲重疊係我支尺第三次報假陽性（標題畫面透過 modal 露出嚟）。
- **量像素 (ADR-162)**：矩形對「鏡頭埋咗入牆」完全盲。抽 framebuffer：正常 34.3／16.6／308 色，埋牆
  25.6／**7.2**／**66**，熄晒燈 **10.3**／21.9／232——**第一版淨用標準差唔響**（熄燈標準差反而升）。
  **對比度唔等於亮度，一個數擔唔起兩種失效**；而家色數 ≥ 120 同亮度 ≥ 18 各守一個。
- **地圖成環 (ADR-161)**：西面庭院本來係死路。拐角第一次砌錯（兩段牆各自穿過對方條走廊）。跟住影相
  係黑嘅而牆表話中間乜都冇——鏡頭遮擋對住 0.42 米厚嘅碰撞盒行而網格厚好多，pad 0.55 → 1.35。
- **死亡畫面 (ADR-160)**：加咗之後全部報重疊，**係我支尺錯**（`<button>` 同入面嘅 `<kbd>R</kbd>`）。
  **閃避 (ADR-159)**：企定 8.89–9.02 %HP/秒、碌 1.78，**剷走無敵幀讀到 1.77**——拆開量先知無敵幀單
  獨有用但加喺位移上面一蚊都唔值，條 gate 誠實講佢守嘅係**位移**。**重開起隱形牆 (ADR-156)**：霧門
  collider 寫咗兩次，gate 加強兩次先捉到。**職業覆蓋 (ADR-158)**：三個職業兩個從來冇載入過。
- **兩把鐘 (ADR-150)**：戰鬥計時器掛喺真實時間而郁動用夾住 0.05 秒嘅 delta，**跌穿 20fps 就唔公平**。
  **揮擊弧線 (ADR-151)** 畫 243° 判定 33°。**霧門通去同一塊地 (ADR-154)**：門檻分唔開兩件企喺同一個
  位嘅嘢，改用 tag。**分區補光 (ADR-155)**：射程外嘅燈 three.js 照樣入 shader 迴圈，熄咗 2.0 → 2.3fps。
- **ER2 起支尺 + MOBA 兩單 (ADR-145–149)**：`.player-hud` 寫死 `top` 而品牌字跟闊度，**844×390 夾唔
  埋**。**Penny 報嘅「技能 CD 卡住」**：兩個冷卻都寫喺「用緊佢嗰條路徑」入面，搬咗上 `step()` loop
  頂——**但個修正令遊戲難咗**：24 局差幅 34 → **45**、**ironhulk 17%**，**近戰一直被呢個 bug 保護**。
  **Hub 兩格一直 404**：`.gitignore` 排除咗兩個 `dist/` 而呢個倉係靜態 Pages。
- **更早 (ADR-135–144)**：一件事寫三次就有三個答案——兩局 5253 下普攻，**588 下（11.2%）播弓弦聲而冇
  嘢飛**。突變掃描：`ai.js` 十四個七殺七生；反轉 `if` 12 殺 8。**玩家郁唔到嘅時間 (ADR-141)**：曲線
  拉平三成半而總時間 **150 對 151 秒**——**個掣係死亡頻率唔係計時器**。

### Earlier checkpoints, in one line each

- 版本標記只覆蓋 67 個請求入面 19 個，而 `cache-bust` 一直綠燈——佢查 `src/` import，唔係有風險嗰半
  （ADR-134）。Hub 圓點 8×8、箭咀 34–42 全部低過自己條 44px（ADR-133）。戰鬥 gate 聲稱角色企喺
  **x = -6**，出手嗰刻其實喺 **x = -62**（ADR-132）。568×320 之下選角**零張完整卡**（ADR-129）。
- Gold：**74.4% 時間買得起但 build list 唔畀買**、**0/72 砌得完**（ADR-130），而**把尺本身就係被調嗰樣
  嘢**（ADR-131）。120 Hz 得 **25.2%** 幀郁過角色，內插後 **97.5%**（ADR-127）。買嘢規則寫咗三次，**淨
  係因為 `canShop` 回 `!!c` 先一致**（ADR-125）；`makeRng` 攞 seed 直接做狀態，**第一個輸出平均 0.007**（ADR-109）。iOS `touch-action: pan-y` 當拖拽係捲動而唔發 `click`（ADR-105/106/107）。

## Verification

- `hub.mjs` **96/96**；ER2 `hud-layout.mjs` **44/44**（五個尺寸版位、flood fill 連通性、牆望唔望得見、
  景物實心、鏡頭遮擋、出生點、時鐘、弧線、boss 招式、路標、分區燈、死完重開）＋ `npm test` **6/6**（`map.test.mjs` 270ms，唔使瀏覽器）。約六
  分鐘。另有 `playthrough.mjs`（唔喺快速套件）。
- **兩個方向都行過**：突變重現到 27% 隱形牆、0/83 網格、霧門兜得過、兩條庭院柱穿得過、0.9 秒出手間隔。
- MOBA：`cache-bust` pass (`assets-28`)、`sim` **262/262**、`browser` **196/196**；`balance 24` **紅色**：ironhulk 17% 低過 20% 線（ADR-146 之後，預期之內，下一輪處理）。

## Changed files

- Hub 檔、`games/moba/*`、`games/elden-ring-ii/{src,tests,dist}`、`scripts/*`、`docs/ai/*.md`。

## Known issues and cautions

- 查過乾淨唔好再推導（ADR-123/129/130/132）：返程被打斷、死住轉向、開商店 GPU lost、`.hidden` 食 tap、
  商店同設定一齊開、420 金上限冇觸發、版位 gate 個 900ms 等待冇漂移。
  仲係去 Google 攞字體。**ER2 改完 `src/` 一定要 `npm run build`**，因為 Pages 派嘅係 `dist/`。
- **未解嘅**：MOBA 打直取景 gate 飄過兩次；診斷已落，過嗰次讀到 −6.8／−6.8／58，假設未證實。
- Cache token 覆蓋成個 module graph **同 Hub stylesheet**（ADR-111/133）；用
  `node scripts/moba-bump-cache.mjs <token>` 改，唔好手改，`cache-bust.mjs` 一漂移就肥。

## Exact next action

1. **ER2 續攤**：`map.ts` 已經拆得出，下一步係將雜兵／boss 嘅追擊規則抽做純函數，然後喺 Node 度用
   真 cannon-es 行——就答得到「敵人追唔追得到你」（今個 session 先啱啱令庭院同聖所啲柱變實心，而佢哋
   完全冇繞路邏輯）。仲有：聖所除咗 boss 乜都冇——三個場入面最大嗰個，而佢係一條走廊加一隻嘢。第二關（三隻）
   係一級難度階梯（ADR-157）。**ER2 未喺真機試過**——呢度得軟件光柵化三幀，手感量唔到。
2. **MOBA 平衡專場**：ADR-146 之後 ironhulk 17%、差幅 45，成因已知（塔同小兵冷卻修好，環境傷害上升）。
   重新做基準，一次改一樣，每次 ≥24 局；ADR-131：ironward／longshot／ironhulk 係把尺。

## Do not redo

- Hub：唔好返去絕對定位嘅單卡輪播、平台 Gomoku emoji、拉長最後一版。MOBA：唔好由 sim event 攞走英雄／
  技能資料、唔好將技能合返做一個環、唔好回復淨係泉水買嘢、唔好攞 `canShop()` 判位置、唔好再為閒置時間
  調 `RESPAWN_*`（ADR-141）或者重加近戰「接觸時間」gate（ADR-142）。
