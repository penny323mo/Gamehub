# Current cross-agent handoff

Updated: 2026-08-05 (Asia/Macau)
Prepared by: Claude Code (cloud)
Integration branch: `main`
Work branch: `main`
Status: Elden Ring II 條走廊插咗入 boss 場二十米 (ADR-174)；**MOBA balance 仍然紅**

## Current objective

Refine Elden Ring II（畫面、物理、遊戲邏輯、擴地圖）。MOBA balance 仍然欠一個專場。

## Completed

### Elden Ring II：條走廊插咗入 boss 場二十米 (ADR-174)

- boss 撲擊起跳嗰刻鎖死落點、預警圈畫喺嗰度、傷害亦由嗰度計。掃晒第二階段所有撲擊組合：**56.6% 中間有
  嘢擋住**。
- 例子全部指住 `x = ±5.6`——北面通道嘅牆。`HALL.z0` 寫住 `BOSS_SPAWN_Z`，**一個數兼兩份冇關係嘅工**：
  boss 企喺聖所正中 (0, -48)，而聖所南門喺 `NORTH.cz + NORTH.r = -28`，即係兩幅走廊牆過咗道門仲行多二
  十米，**將 boss 場南半邊界開兩份**。由門口度出之後：撲擊被擋 **56.6% → 32.8%**、多咗十個企得到嘅位、
  boss 行得最耐嘅一次 **9.4 秒 → 2.9 秒**——佢一直喺度繞一幅唔應該存在嘅牆。
- 剩返嗰三分一係真掩護，所以修規則唔修地圖：`chooseBossMove` 收多個 `見到落點`，見唔到就唔撲。
- 條不變量唔係「走廊幾長」，係**一個場入面唔可以有唔屬於佢自己嗰道環牆嘅牆**。40 毫秒嘅 Node 檢查一跑
  就順手捉埋西面同一單：`BRIDGE.x0` 寫死 `-47` 而庭院東邊喺 `-43`——欄杆插咗入庭院四米。兩邊而家都由佢
  哋通去嗰個場度出；兩個突變各自報返自己個場。

### 上一輪：淨係修咗一邊個牆 (ADR-173)

- ADR-172 自己寫住「兩邊一齊錯」，跟住就淨係畀咗視線玩家嗰邊。企喺障礙兩邊又喺射程之內嘅位置，**雜兵
  85/85、boss 一階 128/128、二階 196/196 全部照打得中**——掩護對玩家值 100%、對敵人值 0%，**比兩邊一齊
  錯仲差**（著數嗰邊係玩家，佢自己唔會發現）。
- 一條 `canLand(from, to, reach, sight)` 三個出手點一齊用；**撲擊由落點度起**（你避嘅係個圈唔係隻怪），
  所以條視線都由同一點度。612 個取樣位、兩個方向都守，射程由遊戲出。

### 再上一輪：啲掩護喺戰鬥入面一直等於唔存在 (ADR-172)

- `findSweptAttackTarget` 同 `nearestEnemy` **都冇問過中間有冇嘢**——場入面每條柱、每嚿石、每幅牆喺戰鬥
  入面都係裝飾。呢個係 ADR-165 條線嘅最後一橛：啲裝飾今個 session 先變實心，之前連行都行得過。
- `makeLineOfSight(boxes)` 線段每 **0.3 米**取樣（薄過最薄嗰塊牆嘅 0.42 半厚）。`nearestEnemy(需要視線)`
  **淨係射嘢嗰邊傳 true**——箭唔應該穿過條柱，但鏡頭唔應該因為對方閃入柱後面就即刻甩鏡。突變**乜都話擋
  住**嗰次，兩個遠程職業連第一波都清唔到——即係條 gate 讀嗰個 seam 同遊戲行嗰條路係同一條。

### 之前：聖所係最疏嗰個場，剩返一個未用過嘅資產 (ADR-171)

- 「聖所除咗 boss 冇嘢」講咗幾輪，量返出嚟**理由錯、結論啱**：圓場 **0.96 件/100m²**（15 件 9 種）、庭
  院 **0.55**、聖所 **0.48**（6 件、**得 2 種**）——最疏**同埋最重覆**，唔係空。
- 逐個環境模型數返用咗幾多次：**得一個 ship 咗但由頭到尾冇出現過** — `bridge-straight-pillar`。ADR-161
  擺過佢落走廊中線，橋面喺人頭高度、由橋底穿過去讀落係一堵牆——**唔啱做路，啱做廢墟**：擺喺 boss 背後
  貼北牆 (0, -63)。實心，准唔准由 gate 決定：boss 探點 41 → 38，仍然**0 個到唔到**。
- **講明**：倉入面而家零個未用資產，而現有嘅唔准重覆用——**唔加新美術嘅話畫面擴張到此為止**。

### 更早：checkpoint 被壓住 + boss 場四分一安全區 (ADR-169/170)

- 恩典點兩個**都喺 collider 入面**：(9, 15) 今日之前就封住，(-52.5, -6.5) 係令庭院啲柱變實心先封死。郁
  裝飾唔郁恩典點。**未做**：`restart()` 將 `encounterStage` 歸零，即係**死喺 boss 手上要由第一波重打**。
- boss 本來零迴避。**第一次量返嚟 0/41 而嗰個係錯嘅**：`追擊試()` 行有迴避嘅規則而 boss 自己嗰段冇——
  **我量咗一隻遊戲入面唔存在嘅 boss**。真數 **10/41 到唔到**，全部停喺柱嘅內側，即係**boss 場四分一係
  安全區**；冇人要求 boss 追到你，所以打完成隻遊戲都唔會發現。而家 0/41。

### 之前：18 個位雜兵追唔到 + 個場搬出 React effect (ADR-167/168)

- 每條關於地圖嘅問題都要開真瀏覽器答，因為個場住喺 `useEffect` 入面——一秒三幀，「敵人追唔到我」同「地
  圖爛咗」係同一個量度。`src/map.ts` 而家淨係常數 + `buildMap(add)`，**唔 import three 亦唔 import
  cannon-es**；搬完 dump 出嚟嗰 111 個盒**逐字一樣**。`tests/map.test.mjs` 幾十毫秒（瀏覽器套件六分半）
  就答到形狀問題；佢頭三條入面兩條第一次跑就紅，兩條都係支尺自己錯。
- `__ER2.追擊試()` 用真 collider 表開即棄 `CANNON.World`，固定步長、乜都唔畫——233 次追擊四秒跑完。
  **18 個位永遠到唔到**，全部係 ADR-165 先變實心嘅裝飾；清晒一波先開到下一關，即係**成局玩唔落去**。
  而家望前 1.6 米、擋住試到 ±2.45 弧度，再加**揀咗邊就唔好變**。18 → **0**。

### 最早嗰兩輪：撞到嘅牆望唔見、望到嘅牆停唔到 (ADR-165/166)

- **93 個 collider、612.2 米，187.6 米（30.6%）一米半內冇任何睇得見嘅嘢**，85 個環牆 collider 更加冇網
  格：地圖輪廓寫咗兩次。而家 **collider 表本身就係網格表**。反方向：(9, 15) 有盒冇模型、**同一個模型一
  個場實心一個場穿得過**；collider 由模型自己 **2 米以下嗰截**度。**走廊裝飾牆內面喺 collider 面入面
  1.36 米**：`鋪一排()` 由走廊自己嗰組數擺位，28 → 60 塊、差 → 0。
- **霧門一直攔緊空氣**：地圖成環之後多咗第二條路入 boss 場，**一關都唔使打就入到**。而家兩道門。
- **支尺自己錯咗四次**：轉角約定鏡像咗（五份抄本一齊錯——**鏡像返轉頭嘅環仲係一個環**）；分界線判斷冇
  限 x；出手節奏 gate 一下出手值 0.26/秒而門檻 0.9；ADR-166 條 gate 符號寫反。

### 更早嘅檢查點

- ADR-105 至 164 全文喺 `DECISIONS.md`。要記住嘅三句：**同一件事寫兩次就有兩個答案**、**一條分唔開兩
  件嘢嘅門檻等於冇門檻**、**綠嘅 gate 未跑過突變之前咩都證明唔到**（今個 session 捉到八次支尺自己錯）。

## Verification

- `hub.mjs` **96/96**；ER2 `hud-layout.mjs` **52/52**（五尺寸版位、flood fill 連通性、牆望唔望得見、景
  物實心、視線、追擊到唔到、鏡頭遮擋、出生點、恩典點、時鐘、弧線、boss 招式、路標、分區燈、死完重開）
  ＋ `npm test` **7/7**（`map.test.mjs` 270ms，唔使瀏覽器）。約六分半鐘。另有 `playthrough.mjs`：bot
  36 秒清第一波、第二波死，同基準一樣（**印數，唔判斷**）。
- **兩個方向都行過**：突變重現到 27% 隱形牆、0/83 網格、霧門兜得過、18 個追唔到嘅位、0.9 秒出手間隔、
  27 件障礙全部「斷唔到」。
- MOBA：`cache-bust` pass、`sim` **262/262**、`browser` **196/196**；`balance 24` **紅色**（ironhulk 17% 低過 20% 線，ADR-146 之後預期之內）。

## Changed files

- Hub 檔、`games/moba/*`、`games/elden-ring-ii/{src,tests,dist}`、`scripts/*`、`docs/ai/*.md`。

## Known issues and cautions

- 查過乾淨唔好再推導（ADR-123/129/130/132）：返程被打斷、死住轉向、開商店 GPU lost、`.hidden` 食 tap、
  商店同設定一齊開、420 金上限冇觸發、版位 gate 個 900ms 等待冇漂移。仲係去 Google 攞字體。
  **ER2 改完 `src/` 一定要 `npm run build`**，因為 Pages 派嘅係 `dist/`。
- **未解嘅**：MOBA 打直取景 gate 飄過兩次；診斷已落，過嗰次讀到 −6.8／−6.8／58，假設未證實。
- Cache token 覆蓋成個 module graph **同 Hub stylesheet**（ADR-111/133）；用
  `node scripts/moba-bump-cache.mjs <token>` 改，唔好手改。

## Exact next action

1. **ER2 續攤**：資產用晒（ADR-171），死咗返第一波（ADR-170），兩樣都要 Penny 落決定。**ER2 未喺真機
   試過**——呢度得軟件光柵化三幀，手感量唔到，而視線加咗之後掩護第一次真係有用，最好真人試。
2. **MOBA 平衡專場**：ADR-146 之後 ironhulk 17%、差幅 45，成因已知（塔同小兵冷卻修好，環境傷害上升）。
   重新做基準，一次改一樣，每次 ≥24 局；ADR-131：ironward／longshot／ironhulk 係把尺。

## Do not redo

- Hub：唔好返去絕對定位嘅單卡輪播、平台 Gomoku emoji、拉長最後一版。MOBA：唔好由 sim event 攞走英雄／
  技能資料、唔好將技能合返做一個環、唔好回復淨係泉水買嘢、唔好攞 `canShop()` 判位置、唔好再為閒置時間
  調 `RESPAWN_*`（ADR-141）或者重加近戰「接觸時間」gate（ADR-142）。
