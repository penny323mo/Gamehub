# Current cross-agent handoff

Updated: 2026-08-05 (Asia/Macau)
Prepared by: Claude Code (cloud)
Integration branch: `main`
Work branch: `main`
Status: Elden Ring II 聖所係最疏嗰個場，而倉入面剩返一個未用過嘅資產 (ADR-171)；**MOBA balance 仍然紅**

## Current objective

Refine Elden Ring II（畫面、物理、遊戲邏輯、擴地圖）。MOBA balance 仍然欠一個專場。

## Completed

### Elden Ring II：聖所係最疏嗰個場，而剩返一個未用過嘅資產 (ADR-171)

- 「聖所除咗 boss 冇嘢」呢句講咗幾輪，量返出嚟**理由錯、結論啱**：貼地過米高嘅裝飾，圓場 **0.96 件/
  100m²**（15 件 9 種）、庭院 **0.55**（5 件 4 種）、聖所 **0.48**（6 件、**得 2 種**：四條柱、兩堆碎
  石）。即係 boss 場係最疏**同埋最重覆**嗰個，唔係空。
- 最順手嗰個做法（再抄幾條柱）正正係唔准做嗰樣。逐個環境模型數返用咗幾多次：**得一個 ship 咗但由頭到
  尾冇出現過** — `bridge-straight-pillar`。ADR-161 擺過佢落西面走廊中線，影出嚟橋面喺人頭高度、玩家由
  橋底穿過去讀落係一堵牆——**唔啱做路，啱做廢墟**：擺喺 boss 背後貼北牆 (0, -63)，你喺佢面前打。
- 實心（同樣係「2 米以下嗰截」規則），准唔准由 gate 決定：boss 追擊探點由 41 跌到 38（三個而家喺廢墟
  入面），仍然**0 個到唔到**；聖所密度 0.48 → 0.56。
- **講明**：倉入面而家零個未用資產，而現有嘅唔准重覆用——**唔加新美術嘅話畫面擴張到此為止**，再擴要麼
  加新資產，要麼用唔靠模型嘅幾何。

### 上一輪：兩個 checkpoint 都被嘢壓住 (ADR-170)

- ADR-165 嗰一個改動嘅**第三個後果**（前兩個：18 個雜兵追唔到嘅位、boss 場四分一安全區）。恩典點係唯
  一嘅回血同 checkpoint，(9, 15) 同 (-52.5, -6.5) **兩個都喺 collider 入面**。(9, 15) **今日之前就封住**
  （手寫障礙盒壓喺上面，而 ADR-165 掃「有 collider 冇模型」就係喺嗰個位補咗嚿石——隱形嘢變成睇得見嘅
  石，一樣壓住個 checkpoint）；(-52.5, -6.5) 係今日令庭院啲柱變實心先封死。企唔到人嘅 checkpoint 唔會
  報錯，佢淨係唔存在。郁裝飾唔郁恩典點：石去 (15.5, 12)，柱去 (-53, ±9.6)。
- **未做，寫低**：`restart()` 將 `encounterStage` 歸零，即係**死喺 boss 手上要由第一波重打**。改「死咗
  之後去邊」係連住難度曲線嘅設計決定（ADR-141 量過一次）。

### 再上一輪：boss 場有四分一係安全區 (ADR-169)

- boss 本來零迴避，而聖所自 ADR-165 起有四條實心柱。**第一次量返嚟 0/41 而嗰個係錯嘅**：`追擊試()` 行
  嘅係有迴避嘅 `chaseDirection`，boss 自己嗰段冇——**我量咗一隻遊戲入面唔存在嘅 boss**；捉到佢淨係因為
  「零迴避竟然一個都唔卡」呢個數靚得唔合理。真數：**41 個位入面 10 個到唔到**，十個都停喺 **x = ±4.3**
  （四條柱嘅內側）。呢個唔會卡死成局，所以打完成隻遊戲都唔會發現——佢淨係令個結局免費。而家 **0/41**。

### 之前：18 個位雜兵追唔到 + 個場搬出 React effect (ADR-167/168)

- 每條關於地圖嘅問題都要開真瀏覽器答，因為個場住喺 `useEffect` 入面——一秒三幀、角色一秒行半米，「敵人
  追唔到我」同「地圖爛咗」係同一個量度。`src/map.ts` 而家淨係常數 + `buildMap(add)`，**唔 import three
  亦唔 import cannon-es**；搬完 dump 出嚟嗰 111 個盒**逐字一樣**。`tests/map.test.mjs` **270 毫秒**（瀏
  覽器套件六分半）問：三個場到唔到、可行區域**封唔封得住**、環牆開口數目同角度。三條入面兩條第一次跑
  就紅，兩條都係支尺（探點喺格網外面、掃描由角度 0 斬開咗東門）。
- `__ER2.追擊試()` 用真 collider 表開即棄 `CANNON.World`，固定 1/60 步長、乜都唔畫——233 次追擊一個
  `evaluate()` **四秒**跑完。**233 個位入面 18 個永遠到唔到**，雜兵停死喺 (±11.2, -11.6)、(13, 8)、
  (-68, -8)——**全部係 ADR-165 先啱啱變實心嘅裝飾**；清晒一波先開到下一關，即係**成局玩唔落去**。
- 而家望前 1.6 米、擋住就試到 ±2.45 弧度攞第一個通嘅——修好 17 個。最後嗰個要**揀咗邊就唔好變**（每步
  重新揀會喺障礙面前左右擺，而庭院嗰嚿石個 collider **8.7 米闊**、望前得 1.6 米）。18 → **0**。

### 更早：撞到嘅牆望唔見、望到嘅牆停唔到 (ADR-165/166)

- **93 個 collider、612.2 米，187.6 米（30.6%）一米半內冇任何睇得見嘅嘢**，85 個環牆 collider 更加冇網
  格：地圖輪廓寫咗兩次。而家 **collider 表本身就係網格表**。反方向：(9, 15) 有盒冇模型、**同一個模型一
  個場實心一個場穿得過**；collider 而家由模型自己 **2 米以下嗰截**度（成個 AABB 唔得，樹嘅 AABB 係樹
  冠）。**走廊裝飾牆內面喺 collider 面入面 1.36 米**：`鋪一排()` 由走廊自己嗰組數擺位、鋪 `round(長/
  闊)` 塊，28 → 60 塊、差 → 0.00。
- **霧門一直攔緊空氣**：地圖成環之後多咗第二條路入 boss 場，**一關都唔使打就入到**。舊 gate 淨係沿 x=0
  行——即係淨係行霧門嗰條路。而家兩道門、一份尺寸、一齊升降。
- **支尺自己錯咗四次，全部靠突變捉到**：轉角約定鏡像咗（五份抄本一齊錯——**鏡像返轉頭嘅環仲係一個
  環**）；分界線判斷冇限 x；出手節奏 gate 一下出手值 0.26/秒而門檻 0.9；ADR-166 條 gate 符號寫反。

### 更早嘅檢查點（全文喺 `DECISIONS.md`）

- **右上角嗰疊 (ADR-163/164)**：`ENTER THE VEIL` 兩個手機打橫得 **30px**、♪ © 全部 **32×32**；加大之後
  又整爛咗 HUD（右邊三個唔相干嘅常數，**本來已經疊住**）。**呢一輪嘅缺陷係呢一輪自己個修正整出嚟。**
- **量像素 (ADR-162)**：正常 34.3／16.6／308 色，埋牆 25.6／**7.2**／**66**，熄燈 **10.3**／21.9／232
  ——**淨用標準差唔響**。**閃避 (ADR-159)**：企定 8.89–9.02 %HP/秒、碌 1.78，**剷走無敵幀讀到 1.77**。
  **兩把鐘 (ADR-150)**：戰鬥計時器掛真實時間而郁動用夾 0.05 秒嘅 delta，**跌穿 20fps 就唔公平**。
- **ER2 起支尺 + MOBA (ADR-135–149)**：`.player-hud` 寫死 `top` 而品牌字跟闊度，**844×390 夾唔埋**。
  **Penny 報嘅「技能 CD 卡住」**：冷卻寫咗喺「用緊佢嗰條路徑」入面，搬上 `step()` loop 頂——**但個修正
  令遊戲難咗**：差幅 34 → **45**、**ironhulk 17%**。**Hub 兩格 404**：`.gitignore` 排除咗 `dist/`。
  **玩家郁唔到嘅時間 (ADR-141)**：曲線拉平三成半而總時間 **150 對 151 秒**——**個掣係死亡頻率唔係計時
  器**；兩局 5253 下普攻**588 下（11.2%）播弓弦聲而冇嘢飛**。
- Gold：**74.4% 時間買得起但 build list 唔畀買**、**0/72 砌得完**（ADR-130），而**把尺本身就係被調嗰樣
  嘢**（ADR-131）。戰鬥 gate 聲稱角色企喺 **x = -6**，出手嗰刻其實喺 **x = -62**（ADR-132）。Hub 圓點
  8×8、箭咀 34–42 低過自己條 44px（ADR-133）。120 Hz 得 **25.2%** 幀郁過角色，內插後 **97.5%**
  （ADR-127）。`makeRng` 攞 seed 直接做狀態，**第一個輸出平均 0.007**（ADR-109）。iOS
  `touch-action: pan-y` 當拖拽係捲動而唔發 `click`（ADR-105/106/107）。

## Verification

- `hub.mjs` **96/96**；ER2 `hud-layout.mjs` **47/47**（五個尺寸版位、flood fill 連通性、牆望唔望得見、
  景物實心、鏡頭遮擋、出生點、時鐘、弧線、boss 招式、路標、分區燈、死完重開）＋ `npm test` **6/6**（`map.test.mjs` 270ms，唔使瀏覽器）。約六
  分鐘。另有 `playthrough.mjs`（唔喺快速套件）。
- **兩個方向都行過**：突變重現到 27% 隱形牆、0/83 網格、霧門兜得過、兩條庭院柱穿得過、0.9 秒出手間隔。
- MOBA：`cache-bust` pass (`assets-28`)、`sim` **262/262**、`browser` **196/196**；`balance 24` **紅色**：ironhulk 17% 低過 20% 線（ADR-146 之後，預期之內，下一輪處理）。

## Changed files

- Hub 檔、`games/moba/*`、`games/elden-ring-ii/{src,tests,dist}`、`scripts/*`、`docs/ai/*.md`。

## Known issues and cautions

- 查過乾淨唔好再推導（ADR-123/129/130/132）：返程被打斷、死住轉向、開商店 GPU lost、`.hidden` 食 tap、
  商店同設定一齊開、420 金上限冇觸發、版位 gate 個 900ms 等待冇漂移。仲係去 Google 攞字體。
  **ER2 改完 `src/` 一定要 `npm run build`**，因為 Pages 派嘅係 `dist/`。
- **未解嘅**：MOBA 打直取景 gate 飄過兩次；診斷已落，過嗰次讀到 −6.8／−6.8／58，假設未證實。
- Cache token 覆蓋成個 module graph **同 Hub stylesheet**（ADR-111/133）；用
  `node scripts/moba-bump-cache.mjs <token>` 改，唔好手改，`cache-bust.mjs` 一漂移就肥。

## Exact next action

1. **ER2 續攤**：聖所除咗 boss 乜都冇——三個場入面最大嗰個。死咗返第一波（見上）。**ER2 未喺真機試
   過**——呢度得軟件光柵化三幀，手感量唔到。
2. **MOBA 平衡專場**：ADR-146 之後 ironhulk 17%、差幅 45，成因已知（塔同小兵冷卻修好，環境傷害上升）。
   重新做基準，一次改一樣，每次 ≥24 局；ADR-131：ironward／longshot／ironhulk 係把尺。

## Do not redo

- Hub：唔好返去絕對定位嘅單卡輪播、平台 Gomoku emoji、拉長最後一版。MOBA：唔好由 sim event 攞走英雄／
  技能資料、唔好將技能合返做一個環、唔好回復淨係泉水買嘢、唔好攞 `canShop()` 判位置、唔好再為閒置時間
  調 `RESPAWN_*`（ADR-141）或者重加近戰「接觸時間」gate（ADR-142）。
