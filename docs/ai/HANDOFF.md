# Current cross-agent handoff

Updated: 2026-08-05 (Asia/Macau)
Prepared by: Claude Code (cloud)
Integration branch: `main`
Work branch: `main`
Status: Elden Ring II 有 18 個位雜兵永遠追唔到，成局卡死 (ADR-168)；**MOBA balance 仍然紅**

## Current objective

Make the MOBA hold up on Penny's phone. Not finished; this is a tested checkpoint.

## Completed

### Elden Ring II：有 18 個位玩家企得到而雜兵永遠追唔到 (ADR-168)

- ADR-167 講拆 `map.ts` 係為咗答「敵人追唔追得到你」。答案好衰，而且**係 ADR-165 整出嚟嘅**。追擊規則
  抽咗做 `src/chase.ts`，`__ER2.追擊試()` 用**真 collider 表**開一個即棄 `CANNON.World`，固定 1/60 步
  長行、乜都唔畫——233 次追擊一個 `evaluate()` **四秒**跑完，同幀率完全無關（呢樣就係八輪嚟答唔到嘅原因）。
- 撒玩家企得到嘅位落每一波嘅場：**233 個入面 18 個永遠到唔到**。雜兵停死喺 (±11.2, -11.6)、(13, 8)、
  (-68, -8)——**全部係今日先啱啱由 ADR-165 變實心嘅裝飾**。而清晒一波先開到下一關，所以嗰啲位唔係
  「打得輕鬆啲」，係**成局玩唔落去**。
- 條規則本來零迴避。而家望前 1.6 米，擋住就試到 ±2.45 弧度，攞第一個通嘅——冇嘢擋嗰陣第一個試嘅就係
  直路，所以空地行為冇變。呢步修好 18 個入面 17 個。
- 最後嗰個要嗰樣最容易漏嘅嘢：**揀咗邊就唔好變**。每步重新揀就會喺障礙面前左右擺，而庭院嗰嚿石個
  collider **8.7 米闊**、望前得 1.6 米。記住上一步嘅轉向之後就沿住個障礙滑出去。18 → **0**，最耐嗰次
  16.3 秒（budget 24）。
- **寫低唔靜靜雞改**：嗰嚿石個 collider 8.7 × 9.3 米（「2 米以下嗰截」出嘅——`rocks-large` 係一堆散
  石），而佢取代嗰個手寫盒得 4.2 米；模型望落 14 米闊，最外圈啲石仲係穿得過。

### 之前：個場搬出 React effect，喺 Node 度 270 毫秒量得晒 (ADR-167)

- 每一條關於地圖嘅問題都要開真瀏覽器答，因為個場住喺 `useEffect` 入面。一秒三幀、角色一秒行半米，所以
  「敵人追唔到我」同「地圖爛咗」係同一個量度，而鏡像約定捱咁耐都係因為**得一把尺而嗰把尺貴到冇人肯行
  第二次**。`src/map.ts` 而家淨係常數 + `buildMap(add)`，**唔 import three 亦唔 import cannon-es**；
  搬完由行緊嘅遊戲 dump 出嚟嗰 111 個盒**逐字一樣**。
- `tests/map.test.mjs` **270 毫秒**（瀏覽器套件六分半），問三樣之前貴到唔問得嘅：三個場到唔到、可行區
  域**封唔封得住**（地板係無限平面，從來冇問過）、每道環牆嘅開口數目同角度。三條入面兩條第一次跑就
  紅，兩條都係支尺：探點跌咗喺格網外面（索引滑咗去隔籬行）、掃描由角度 0 開始斬開咗跨 0 嗰個東門。

### 再之前：撞到嘅牆望唔見、望到嘅牆停唔到 (ADR-165/166)

- **93 個 collider、612.2 米，187.6 米（30.6%）一米半內冇任何睇得見嘅嘢**，20 個成條長度隱形。地圖輪
  廓寫咗兩次：collider 由 `BRIDGE`／`HALL`／`LINK` 生出，畫面嗰啲 `wall.glb` 係另一張手寫表（四個位隔
  7 米而模型闊 3.97 米），85 個環牆 collider 更加冇網格。而家 **collider 表本身就係網格表**。
- **反方向**：(9, 15) 有盒冇模型，而**同一個模型一個場實心一個場穿得過**。collider 而家由模型自己
  **2 米以下嗰截**度（成個 AABB 唔得，樹嘅 AABB 係樹冠）。
- **霧門一直攔緊空氣**：地圖成環之後多咗第二條路入 boss 場，**一關都唔使打就入到**。舊 gate 淨係沿
  x=0 行，即係淨係行霧門嗰條路。而家兩道門、一份尺寸、一齊升降。
- **走廊裝飾牆內面喺 collider 面入面 1.36 米**（ADR-166）。`鋪一排()` 由走廊自己嗰組數擺位、量返模型
  幾深、鋪 `round(長/闊)` 塊。28 → 60 塊，差 → 0.00；郁動秒 6.0 → 5.9（**低過 2%**）。
- **支尺自己錯咗四次，全部靠突變捉到**：轉角約定鏡像咗（五份抄本一齊錯——**鏡像返轉頭嘅環仲係一個
  環**，所以淨係問「封唔封得住」永遠答啱）；分界線判斷冇限 x；出手節奏 gate 一下出手值 0.26/秒而門檻
  0.9（3 下綠 4 下紅）；ADR-166 條 gate 符號寫反，突變之下照綠。

### 之前幾輪：版位、像素、地圖成環、時鐘 (ADR-150–164)

- **右上角嗰疊 (ADR-163/164)**：`ENTER THE VEIL` 兩個手機打橫得 **30px**、♪ 同 © 全部 **32×32**，高度
  係副產品。加大之後又整爛咗 HUD——右邊三個唔相干嘅常數，**本來已經疊住**只係啱好未掂到字。**呢一輪嘅
  缺陷係呢一輪自己個修正整出嚟。** credits 嗰啲重疊係支尺第三次假陽性。
- **量像素 (ADR-162)**：矩形對「鏡頭埋咗入牆」完全盲。抽 framebuffer：正常 34.3／16.6／308 色，埋牆
  25.6／**7.2**／**66**，熄燈 **10.3**／21.9／232——**淨用標準差唔響**（熄燈反而升）。而家色數 ≥ 120
  同亮度 ≥ 18 各守一個。**地圖成環 (ADR-161)**：拐角第一次砌錯；鏡頭遮擋 pad 0.55 → 1.35。
- **死亡畫面 (ADR-160)** 全部報重疊，**係支尺錯**。**閃避 (ADR-159)**：企定 8.89–9.02 %HP/秒、碌 1.78，
  **剷走無敵幀讀到 1.77**——無敵幀加喺位移上面一蚊都唔值。**重開起隱形牆 (ADR-156)**：霧門 collider
  寫咗兩次。**職業覆蓋 (ADR-158)**：三個職業兩個從來冇載入過。
- **兩把鐘 (ADR-150)**：戰鬥計時器掛真實時間而郁動用夾 0.05 秒嘅 delta，**跌穿 20fps 就唔公平**。
  **弧線 (ADR-151)** 畫 243° 判定 33°。**霧門通去同一塊地 (ADR-154)**：門檻分唔開同位置嘅兩件嘢，改
  tag。**分區補光 (ADR-155)**：射程外嘅燈照樣入 shader 迴圈，熄咗 2.0 → 2.3fps。
- **ER2 起支尺 + MOBA (ADR-145–149)**：`.player-hud` 寫死 `top` 而品牌字跟闊度，**844×390 夾唔埋**。
  **Penny 報嘅「技能 CD 卡住」**：兩個冷卻寫喺「用緊佢嗰條路徑」入面，搬咗上 `step()` loop 頂——**但個
  修正令遊戲難咗**：差幅 34 → **45**、**ironhulk 17%**，**近戰一直被呢個 bug 保護**。**Hub 兩格 404**：
  `.gitignore` 排除咗兩個 `dist/`。
- **更早 (ADR-135–144)**：兩局 5253 下普攻，**588 下（11.2%）播弓弦聲而冇嘢飛**；突變掃描 `ai.js`
  十四個七殺七生。**玩家郁唔到嘅時間 (ADR-141)**：曲線拉平三成半而總時間 **150 對 151 秒**——**個掣係
  死亡頻率唔係計時器**。

### Earlier checkpoints, in one line each

- 版本標記只覆蓋 67 個請求入面 19 個，而 `cache-bust` 一直綠燈——佢查 `src/` import，唔係有風險嗰半
  （ADR-134）。Hub 圓點 8×8、箭咀 34–42 全部低過自己條 44px（ADR-133）。戰鬥 gate 聲稱角色企喺
  **x = -6**，出手嗰刻其實喺 **x = -62**（ADR-132）。568×320 之下選角**零張完整卡**（ADR-129）。
- Gold：**74.4% 時間買得起但 build list 唔畀買**、**0/72 砌得完**（ADR-130），而**把尺本身就係被調嗰樣
  嘢**（ADR-131）。120 Hz 得 **25.2%** 幀郁過角色，內插後 **97.5%**（ADR-127）。買嘢規則寫咗三次，**淨
  係因為 `canShop` 回 `!!c` 先一致**（ADR-125）；`makeRng` 攞 seed 直接做狀態，**第一個輸出平均 0.007**（ADR-109）。iOS `touch-action: pan-y` 當拖拽係捲動而唔發 `click`（ADR-105/106/107）。

## Verification

- `hub.mjs` **96/96**；ER2 `hud-layout.mjs` **45/45**（五個尺寸版位、flood fill 連通性、牆望唔望得見、
  景物實心、鏡頭遮擋、出生點、時鐘、弧線、boss 招式、路標、分區燈、死完重開）＋ `npm test` **6/6**（`map.test.mjs` 270ms，唔使瀏覽器）。約六
  分鐘。另有 `playthrough.mjs`（唔喺快速套件）。
- **兩個方向都行過**：突變重現到 27% 隱形牆、0/83 網格、霧門兜得過、兩條庭院柱穿得過、0.9 秒出手間隔。
- MOBA：`cache-bust` pass (`assets-28`)、`sim` **262/262**、`browser` **196/196**；`balance 24` **紅色**：ironhulk 17% 低過 20% 線（ADR-146 之後，預期之內，下一輪處理）。

## Changed files

- Hub 檔、`games/moba/*`、`games/elden-ring-ii/{src,tests,dist}`、`scripts/*`、`docs/ai/*.md`。

## Known issues and cautions

- 查過乾淨唔好再推導（ADR-123/129/130/132）：返程被打斷、死住轉向、開商店 GPU lost、`.hidden` 食 tap、
  商店同設定一齊開、420 金上限冇觸發、版位 gate 個 900ms 等待冇漂移。
  仲係去 Google 攞字體。**ER2 改完 `src/` 一定要 `npm run build`**，因為 Pages 派嘅係 `dist/`。
- **未解嘅**：MOBA 打直取景 gate 飄過兩次；診斷已落，過嗰次讀到 −6.8／−6.8／58，假設未證實。
- Cache token 覆蓋成個 module graph **同 Hub stylesheet**（ADR-111/133）；用
  `node scripts/moba-bump-cache.mjs <token>` 改，唔好手改，`cache-bust.mjs` 一漂移就肥。

## Exact next action

1. **ER2 續攤**：boss 仲係零迴避（`chase.ts` 淨係接咗雜兵）——boss 場而家有四條實心柱。仲有：聖所除
   咗 boss 乜都冇——三個場入面最大嗰個，而佢係一條走廊加一隻嘢。第二關（三隻）
   係一級難度階梯（ADR-157）。**ER2 未喺真機試過**——呢度得軟件光柵化三幀，手感量唔到。
2. **MOBA 平衡專場**：ADR-146 之後 ironhulk 17%、差幅 45，成因已知（塔同小兵冷卻修好，環境傷害上升）。
   重新做基準，一次改一樣，每次 ≥24 局；ADR-131：ironward／longshot／ironhulk 係把尺。

## Do not redo

- Hub：唔好返去絕對定位嘅單卡輪播、平台 Gomoku emoji、拉長最後一版。MOBA：唔好由 sim event 攞走英雄／
  技能資料、唔好將技能合返做一個環、唔好回復淨係泉水買嘢、唔好攞 `canShop()` 判位置、唔好再為閒置時間
  調 `RESPAWN_*`（ADR-141）或者重加近戰「接觸時間」gate（ADR-142）。
