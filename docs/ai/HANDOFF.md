# Current cross-agent handoff

Updated: 2026-08-06 (Asia/Macau)
Prepared by: Claude Code (cloud)
Integration branch: `main`
Work branch: `main`
Status: Elden Ring II 個打擊特效唔知自己代表緊一下打擊 (ADR-181)；**MOBA balance 仍然紅**

## Current objective

Refine Elden Ring II 嘅物理、動作真實感、打擊感。MOBA balance 仍然欠一個專場。

## Completed

### Elden Ring II：個打擊特效唔知自己代表緊一下打擊 (ADR-181)

- **全場得一組碎屑**：`burst()` 每次覆蓋同一個 `THREE.Points`，0.55 秒未散完又有第二下打擊，頭嗰蓬就
  **成蓬瞬移**去新位置。實測真打鬥入面 **七次有兩次（29%）**被搶。而家一個五個嘅池，實測同時最多用兩個。
- **噴濺同打擊方向完全冇關**：水平等向亂數，垂直係 `rand × 4.2`——**永遠 ≥ 0**，42 粒有 39–42 粒向上。
  無論邊個方向斬落去都係同一個噴泉。而家係一個繞住打擊方向嘅錐；集中度由 **0.03–0.11**（噪音）升到
  **0.75–0.93**。**碎屑重力寫住 5**（半空慢動作）——由積分度返，**5.0 → 9.80**。
- **一個要記低嘅坑**：第一次突變跑出嚟 **72/72 全綠**。個突變有 type error、`npm run build` 中止咗，而
  **舊 `dist` 仲喺度**——套件派嘅就係 `dist`，於是佢開開心心咁測咗個冇突變嘅 bundle。**build 失敗會留低
  一個讀落似成功嘅舊產物。** 改到編譯得之後三條全部紅，而且重現到原本個數。
- 鏡頭震仲係固定 0.24（傷害 13 到 22 都一樣）：source 度睇得到，但我冇量過佢係缺陷，冇郁，記低。
  版位 gate **69 → 72**。

### 上一輪：條加速上限一幀行完，隻角色側住身行路 (ADR-180)

- ADR-176 加咗加速上限，我記低咗「修好」。其實冇：`ACCEL = 70` 配全速 4.4 即係斜坡 **0.063 秒**行完，
  而 `delta` 封喺 0.05——**一幀**（實測起步 0.05 秒，7 g）。有上限嘅斜坡同瞬間跳喺加速度嗰把尺入面係同
  一個數；分得開佢哋嗰條問題係「用咗幾耐」，而冇人問過。
- 位移用**想去嗰個方向**，而朝向係另一條有上限嘅線——身體滑向一個佢冇面住嘅方向。實測**玩家側滑 2.0
  弧度（115 度）**、雜兵 0.43。而家一條 `gaitStep`（轉身 → 沿住面向行 → 入彎收力），玩家、雜兵、boss、
  `追擊試` 全部行同一條（seam 行唔同規則＝量緊一隻唔存在嘅雜兵）。`ACCEL` 70 → 8，側滑 → **0.00**。
- **三把尺量緊唔係自己個名嗰件事**：加速 gate 讀到 **41.4 對上限 14**，由位置差度——而位置差入面有牆滑
  行、重力、同**雜兵剛體接觸推撞**（被人推唔係你嘅加速度）；節奏 gate 個 32 秒窗口收場係 `狀態: "dead"`
  ——**量緊一具屍體**；`打得死嘢` 條線（四下命中 68 對兩隻雜兵 70 血）**細過佢要守嗰件事**，改成量傷害。
- **一條寫完又剷咗嘅 gate**：瀏覽器度量斜坡用時，健康 0.5 秒、ACCEL 揼返 70 讀到 0.3 秒——又要喺噪音
  中間揀線；同一問題喺純函數 test 度係 **0.067 對 0.55**。而瀏覽器 gate 結構上答唔到「個常數本身合唔
  合理」（佢同遊戲自己個常數比），所以人形尺度嗰條線寫咗喺 Node 嗰邊。

### 之前：射手側住身放箭，而我第一把尺讀錯咗自己個分母 (ADR-179)

- 出手轉向寫住 `if (locked && …)`，而 `locked` 開波係 `true`——**六十四條 gate 全部行喺鎖定狀態，
  嗰個條件嘅 `false` 邊從來冇行過**。我第一版把尺喺鎖定嘅頁度量，剷走個 `locked &&` 個數一模一樣。
  撳咗 Q 先量到真嘢：**落點嗰刻朝向仲差 0.43／0.39／0.17／0.46 弧度（26 度），同出手嗰刻一個字都
  冇變**——成個前搖冇轉過身。而家有目標就轉，用返 `TURN_RATE_ATTACK`，落點偏差 → **0.00**。
- **支箭插喺離敵人 1.8 米嘅空地上面**，佢照樣扣血：落點喺出手嗰刻抄低就唔再郁。而家跟住活目標。兩把
  尺分開：`箭落差` 量目標飛咗幾遠（同修法無關，唔會空綠），`箭到位` 量支箭停喺邊——1.8 → 0.00。
- **我讀錯咗自己支尺**：`對得上 3/6` 個分母係**發招**唔係**落點**。數返落點，未修嗰個 build 已經係
  **4/4**——兩條規則從來冇對唔上，差嗰啲係畀人打斷。我喺查證之前已經寫咗個「修法」，突變顯示**剷走個
  分支個數一模一樣**，所以剷咗佢。版位 gate **64 → 68**。

### 之前：舊回歸同背景閃爍 (ADR-176/177/178)

- ADR-178：條斜坡讀「現速」係由 physics body 讀返，而每幀開頭有 `velocity.x = 0`——實測**玩家 0.09
  米／秒，設計 12.5**，而**六十二條 gate 全綠**（全部量變化率，冇一條量過個值）。ADR-176：量到轉向
  **62.6 弧度／秒**、加速 **250 米／秒²**，抽咗 `motion.ts` 出嚟；敵人本來每幀準確指住你，**繞後根本
  唔存在**。ADR-177：Penny 報「背景閃爍」，**我重現唔到——閃爍係逐幀嘅嘢，呢個環境一秒三幀**；三個
  已知成因由成因修（陰影貼格、`near` 0.1 → 0.6、同平面）。**仲閃就要知係邊塊面。**

### 之前：手感、走廊、掩護 (ADR-171–175)

- **Penny 落手玩，一句講咗三個缺陷**，而五十二條 gate 一條都捉唔到：佢哋量「隻遊戲係咩」，唔係「攞上
  手係咩感覺」。鏡頭 **2.73–2.84 米**（設計 8.3）→ 出生點改由 `ARENA_RADIUS − CAMERA_CLEARANCE` 出；
  縮放 0.7–1.7；搖桿改浮動。改完即刻整出兩個新缺陷。**條走廊插咗入 boss 場二十米**（`HALL.z0` 寫住
  `BOSS_SPAWN_Z`——一個數兼兩份工），撲擊被擋 56.6% → 32.8%。

### 更早：掩護、追唔到、安全區、牆望唔見 (ADR-165–173)

- **掩護加咗視線之後我淨係修咗玩家嗰邊**：射程之內、視線斷咗嘅位，**雜兵 85/85、boss 128/128 全部照打
  得中**——掩護對玩家值 100%、對敵人值 0%，**比兩邊一齊錯仲差**。而家一條 `canLand()` 三處一齊用。
- **93 個 collider、612.2 米，187.6 米（30.6%）一米半內冇任何睇得見嘅嘢**：地圖輪廓寫咗兩次；而家
  **collider 表本身就係網格表**。**霧門一直攔緊空氣**。個場搬入 `src/map.ts`（**唔 import three**）；
  `追擊試()` 揾到 **18 個位雜兵永遠追唔到**、**boss 場四分一係安全區**。
- **支尺自己錯咗幾次**：轉角約定鏡像咗（五份抄本一齊錯——**鏡像返轉頭嘅環仲係一個環**）；`追擊試()`
  **量咗一隻唔存在嘅 boss**。**未做**：`restart()` 歸零 `encounterStage`＝**死喺 boss 手上重打第一波**。

### 更早嘅檢查點

- ADR-105 至 164 全文喺 `DECISIONS.md`。三句：**同一件事寫兩次就有兩個答案**、**一條分唔開兩件嘢嘅
  門檻等於冇門檻**、**綠嘅 gate 未跑過突變之前咩都證明唔到**（今個 session 捉到九次支尺自己錯）。

## Verification

- `hub.mjs` **96/96**；ER2 `hud-layout.mjs` **72/72** ＋ `npm test` **16/16**（`map.test.mjs` 幾十毫秒，
  唔使瀏覽器）。約七分鐘。另有 `playthrough.mjs`：bot 36 秒清第一波、第二波死（**印數，唔判斷**）。
- **兩個方向都行過**：每條新 gate 都跑過突變，重現到原本量到嗰個數。
- MOBA：`cache-bust` pass、`sim` **262/262**、`browser` **196/196**；`balance 24` **紅色**（ironhulk 17%，ADR-146 之後預期之內）。

## Changed files

- Hub 檔、`games/moba/*`、`games/elden-ring-ii/{src,tests,dist}`、`scripts/*`、`docs/ai/*.md`。

## Known issues and cautions

- 查過乾淨唔好再推導（ADR-123/129/130/132）：返程被打斷、死住轉向、開商店 GPU lost、`.hidden` 食 tap、
  商店同設定一齊開、420 金上限冇觸發、版位 gate 個 900ms 等待冇漂移。仲係去 Google 攞字體。**ER2 改完
  `src/` 一定要 `npm run build`**，因為 Pages 派嘅係 `dist/`。
- **未解嘅**：MOBA 打直取景 gate 飄過兩次；診斷已落，過嗰次讀到 −6.8／−6.8／58，假設未證實。
- Cache token 覆蓋成個 module graph **同 Hub stylesheet**（ADR-111/133）；用
  `node scripts/moba-bump-cache.mjs <token>` 改，唔好手改。

## Exact next action

1. **ER2 續攤**：ADR-175 講明**手感類缺陷淨係真人玩得出**。仲有：資產用晒（ADR-171）、死咗返第一波
   （ADR-170），兩樣都要 Penny 落決定。**下一個候選**：ADR-179 揭咗「條件嘅一邊從來冇行過」呢個形，
   值得掃一次仲有邊啲 gate 係咁（`sprinting`、`phase===2`、觸控路徑）。
2. **MOBA 平衡專場**：ADR-146 之後 ironhulk 17%、差幅 45，成因已知（塔同小兵冷卻修好，環境傷害上升）。
   重新做基準，一次改一樣，每次 ≥24 局；ADR-131：ironward／longshot／ironhulk 係把尺。

## Do not redo

- Hub：唔好返去絕對定位嘅單卡輪播、平台 Gomoku emoji、拉長最後一版。MOBA：唔好由 sim event 攞走英雄／
  技能資料、唔好將技能合返做一個環、唔好回復淨係泉水買嘢、唔好攞 `canShop()` 判位置、唔好再為閒置時間
  調 `RESPAWN_*`（ADR-141）或者重加近戰「接觸時間」gate（ADR-142）。
