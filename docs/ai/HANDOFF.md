# Current cross-agent handoff

Updated: 2026-08-06 (Asia/Macau)
Prepared by: Claude Code (cloud)
Integration branch: `main`
Work branch: `main`
Status: Elden Ring II **通關咗** (ADR-191)：三波加 boss、82 郁動秒、收場 7 血 0 藥

## Current objective

Refine Elden Ring II 嘅物理、動作真實感、打擊感。MOBA balance 仍然欠一個專場。

## Completed

### Elden Ring II：通關咗 (ADR-191)

- `[3s] wave1 血100`　`[8s] wave2 血80`　`[20s] wave3 血87`　`[36s] boss 藥0`　**`[82s] victory 血7`**
  ——三波加 boss，**82 郁動秒**，收場 **7 血、0 藥**、39 下出手、476 傷害。最後十秒唔輕鬆，而第一次通關
  落喺呢個位大致啱。**遊戲本身唔使再改**（ADR-187 至 190 啲修法已經喺度）；要嘅係一個打得郁嘅支尺。
- **個 bot 嘅瓶頸係 round trip**：每個決定一次 `page.evaluate` ≈ 0.3–0.5 **郁動**秒，所以佢一下／2.2 秒
  （遊戲容許 0.87）。個政策搬**入頁面**（90 毫秒 interval、派合成鍵盤事件），一個 run 由幾百個決定變成
  **4600** 個。
- 跟住兩樣攔住佢，**兩樣都係 bot 側，而且睇落好似遊戲 bug**：**冇尋路**（直線行去六十米外嘅 boss，喺
  走廊撞住牆卡咗 **200 秒、boss 100 血冇跌過**——flood-fill gate 一早證明條路通）；**碌到鎖死**（低血之下
  boss 不停舉手，打成 **15 血對 10 血嘅僵局兩百秒**）。加「距離唔再縮短就打橫行」同「佢就快死就唔好再
  碌」就收咗尾。
- 要講白：**之前幾輪我攞住「bot 通唔到關」當成隻遊戲嘅證據，其實嗰個係 bot 嘅證據**。由頭到尾誠實嗰把
  尺係算術。`tests/playthrough-full.mjs` 跑通關、`tests/first-clear.log` 係嗰個 run；佢要成刻鐘，所以唔入
  套件，套件守住佢依賴嗰啲有界不變量。


### 之前：貼身打唔中人、體力經濟 (ADR-189/190)

- 逐下斬記低距離同角度：**4.2 米中、2.4 米中，但 1.6／1.8／1.8 米全部空**——近距離斬空。前搖踏前推你向前
  0.86 米、雜兵同時埋身 0.86 米：**你衝過咗頭，佢跌咗去你身後**。實際輸出 **0.4 dps**（設計 11.9）。
  踏前停喺接觸面之後：**打出傷害 30 → 128**。角度嗰邊：前搖只夠轉 **70°**，空嗰啲全部 148°／156°／180°。
  **我試過修，結果更差**：「轉唔切就唔揀佢」整咗個死鎖，傷害跌返 **17**。剷咗：**轉得慢好過唔轉**。
- **互動輸入畀食咗**：`queuedInteract` 每幀無條件清零而飲藥要求唔喺擊退中——**啱好喺捱打嗰一幀撳 E 就冇
  咗**。而家緩衝 0.45 秒。**同一個「真實秒 vs 郁動時間」嘅窿今個 session 中咗三次**。
- **ADR-190**：**回氣封鎖咗成個出手動畫**（0.39 秒收招期一樣唔回）——持續 **11.9 dps** 對第二波 24.4，
  一格體力買 5 下而一隻雜兵要 3 下。而家只喺前搖封鎖。修完：**13 秒淨係撳攻擊清咗第一波，交換 133 對
  100**。**我信之前讀錯過呢個量度一次**（「146 傷害零擊殺」其實係兩隻都死咗、關卡推咗、我望緊新一批）。

### 之前：藥瓶同雜兵速度 (ADR-187/188)

- **ADR-187**：通唔到關，證明係**遊戲自己啲常數嘅算術**（逐隻打都要 117 傷害對 100 血），而**打緊交冇
  任何回復手段**。加咗**三支藥、每支 55 血、0.95 秒定身**；手機本來**連互動掣都冇**，而家一粒掣兩個用途。
- **ADR-188**：藥瓶加咗但**飲唔到**（bot 想拉開 21 次、成功 **0** 次）：玩家行 4.4 而雜兵 3.6/4.1/4.4,
  **第三波同你一樣快**，而唯一拉得開嘅衝刺**兼封鎖回氣**。改成 73/77/84%。**我支尺當時亦錯緊**：真實毫秒
  落指令，而且 **WASD 係鏡頭相對而 bot 當佢世界座標**。

### 之前：套件慢到開始判錯冇壞嘅嘢 (ADR-186)

- 套件跑到半個鐘之後**唔止係慢**：一次等入場掣 60 秒 timeout 掛咗，另一次兩條紅 gate 而佢哋啲碼冇問題。
  ER2_TIME=1 印每條用幾秒——量完推翻我個估計。兩個成因：**兩個版塊喺主 page 泊低之前跑**；**解析度就係
  幀率，而幀率就係遊戲時間**（640×380 得 1.7 fps、320×190 4.2）。**955 → 619 秒**。**我兩條 gate 亦問錯
  咗問題**：手機衝刺要求埋 Run 動畫（瞬時狀態）；鏡頭收位條線由一次量到嘅 0.004 拍出嚟，改成問「有冇一
  路細落去」之後**又紅多一次**，讀到 0,0,0,0,0,0：**收到盡就唔再細落去**。

### 更早：抖動、boss、搖桿、重開、特效 (ADR-181–185)

- **ADR-185**：Penny 報「畫面一直抖動」，逐幀嘅尺仍然重現唔到，而**我第一個數係我自己支尺錯**。兩個真
  缺陷：**renderer 同 composer 用兩個唔同像素比**（dpr ≤ 1.55 兩個一樣，測試機必然睇唔到，gate 要喺
  dsf 3 跑）；**震動寫咗入平滑狀態**。**未證實**係咪 Penny 見嗰個。
- **ADR-184**：**撲擊喺遊戲入面永遠揀唔到**（規則要 > 6.5，caller 只有 ≤ 3.15；純函數 gate 綠，因為佢餵
  咗遊戲從來唔會餵嘅距離）。**每一下命中都取消緊 boss 出緊嗰招**——打到剩 17 血、到落點 **0** 次。
- **ADR-183**：搖桿幅度算咗又即刻掉咗（輕推同推到底一樣快），衝刺淨係綁 ShiftLeft（手機用唔到）。
  **ADR-182**：restart() 無條件歸零關卡；推關() 即刻捉到一個**我啱啱先寫出嚟**嘅缺陷（畫得出但攔唔到你
  嘅霧門）。**ADR-181**：全場得一組碎屑（29% 畀搶走）、噴濺同打擊方向無關、重力 5 → 9.80。
- **一個中咗兩次嘅坑**：突變有 type error、`npm run build` 中止、**舊 `dist` 仲喺度**，套件測咗個冇突變
  嘅 bundle。**突變之後啲數唔郁，第一件事係查個 build 有冇出過新 bundle。**

### 更早：郁動、回歸、掩護、地圖 (ADR-165–180)

- **ADR-180**：ACCEL 70 配全速 4.4＝斜坡 0.063 秒而 delta 封 0.05——起步 **一幀**；**側滑 2.0 弧度**。
  而家一條 gaitStep 四邊共用。**ADR-179**：出手轉向寫住 locked && … 而 locked 開波 true——**六十四條 gate
  從來冇行過個 false 邊**。
- **ADR-178**：斜坡讀「現速」由 body 讀返而每幀開頭清零——**玩家 0.09 米／秒，設計 12.5**，而**六十二條
  gate 全綠**（全部量變化率）。**ADR-165–177**：Penny 一句講咗三個缺陷而五十二條 gate 捉唔到；HALL.z0
  一個數兼兩份工；掩護**我淨係修咗玩家嗰邊**；**30.6% collider 一米半內冇嘢睇得見**；**霧門一直攔緊
  空氣**；**18 個位雜兵永遠追唔到**；轉角約定五份抄本一齊鏡像咗。

### 更早嘅檢查點

- ADR-105 至 164 全文喺 DECISIONS.md。四句：**同一件事寫兩次就有兩個答案**、**分唔開兩件嘢嘅門檻等於
  冇門檻**、**綠 gate 未跑突變之前咩都證明唔到**、**一個條件嘅一邊從來冇行過就等於冇寫過**。

## Verification

- `hub.mjs` **96/96**；ER2 `hud-layout.mjs` **92/92** ＋ `npm test` **17/17**。ER2 套件約 **十分鐘**
  （ADR-186 由半個鐘減落嚟）。`ER2_TIME=1` 印每條用幾多秒。迭代仍然建議寫專題探針。
- **兩個方向都行過**：每條新 gate 都跑過突變，重現到原本量到嗰個數。
- MOBA：`cache-bust` pass、`sim` **262/262**、`browser` **196/196**；`balance 24` **紅色**（ironhulk 17%，ADR-146 之後預期之內）。

## Changed files

- Hub 檔、`games/moba/*`、`games/elden-ring-ii/{src,tests,dist}`、`scripts/*`、`docs/ai/*.md`。

## Known issues and cautions

- 查過乾淨唔好再推導（ADR-123/129/130/132）：返程被打斷、死住轉向、開商店 GPU lost、.hidden 食 tap、
  商店同設定一齊開、420 金上限冇觸發。**ER2 改完 src/ 一定要 npm run build**（Pages 派 dist/；build 一
  失敗就留低舊 dist，讀落似成功）。
- Cache token 覆蓋成個 module graph **同 Hub stylesheet**（ADR-111/133）；用
  `node scripts/moba-bump-cache.mjs <token>` 改，唔好手改。**未解**：MOBA 打直取景 gate 飄過兩次。

## Exact next action

1. **ER2**：**通關證咗**（ADR-191，`tests/playthrough-full.mjs`）。未量過：鏡頭震固定 0.24、`invincibleUntil`。
   兩個形今個 session 各中三次，值得再掃：**一個條件嘅一邊從來冇行過**（ADR-179／182／184）、**用真實秒
   去等一件行郁動時間嘅事**（ADR-186／187／189）。3D 資產用晒（ADR-171），擴場要新美術。
2. **MOBA 平衡專場**：ADR-146 之後 ironhulk 17%、差幅 45，成因已知（塔同小兵冷卻修好，環境傷害上升）。
   重新做基準，一次改一樣，每次 ≥24 局；ADR-131：ironward／longshot／ironhulk 係把尺。

## Do not redo

- Hub：唔好返去絕對定位嘅單卡輪播、平台 Gomoku emoji、拉長最後一版。MOBA：唔好由 sim event 攞走英雄／
  技能資料、唔好將技能合返做一個環、唔好回復淨係泉水買嘢、唔好攞 `canShop()` 判位置、唔好再為閒置時間
  調 `RESPAWN_*`（ADR-141）或者重加近戰「接觸時間」gate（ADR-142）。
